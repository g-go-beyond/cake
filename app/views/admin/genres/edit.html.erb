<div class='container'>
 <div class='row'>
  <div class='col-8'>
   <h4 class="col-sm-4 bg-light my-4">ジャンル編集</h2>
    <div  class=" row w-100 my-4 mx-4">
     <%= form_with model:@genre, url: {controller: 'genres', action: 'update' }, local:true do |f| %>
      ジャンル名
      <%= f.text_field :name %>
      <%= f.submit "変更を保存" , class: "btn btn-success"%>
     <% end %>
   </div>
  </div>
 </div>
</div>
