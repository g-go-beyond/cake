<p>ようこそ、ながのCAKEへ！</br>
このサイトでは、ケーキ販売のECサイトになります。</br>
会員でないkたも商品の閲覧はできますが、</br>
購入には会員登録が必要になります。</p>
<h2>新着商品</h2>
  <% @items.first(4).each do |item|%>
    <p>
      <%= link_to item_path(item) do %>
       <%= attachment_image_tag(item, :image, fallback: "no_image.jpg", size: "100x120")%>
      <%end%>
    </p>
    <p>商品名　<%=item.name%></p>
    <p>
      <% if item.is_active == TRUE %>
       税込￥<%= item.tax_included_price.to_s(:dalimited) %>
      <% else %><!--modelにて設定↑-->
       "sorry!sold out!"
      <% end %>
    </p>
<%= link_to "全ての商品を見る", items_path　%>
  <% end %>